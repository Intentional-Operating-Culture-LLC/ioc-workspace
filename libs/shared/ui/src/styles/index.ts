/**
 * @fileoverview Styles export for IOC UI package
 * @description Entry point for all shared styles
 */

// Export the global styles path for importing in applications
export const globalStyles = './globals.css';

// CSS files can't be imported in TypeScript, so we just provide the path
// Applications should import the CSS directly using: import './globals.css'