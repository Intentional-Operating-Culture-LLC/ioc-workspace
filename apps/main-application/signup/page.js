import SignupForm from "@ioc/shared/ui";

export const metadata = {
  title: 'Sign Up - IOC Core',
  description: 'Create a new IOC Core account'
};

export default function SignupPage() {
  return <SignupForm />;
}